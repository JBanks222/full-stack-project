# full-stack-project
we'll start with html ,and css and build out all screens
user profiles should be created in the SQL database
then connect profiles (username + password) to app.py 
then the image upload url sql column
then connect to app.py
then do the same for to do list and blog


